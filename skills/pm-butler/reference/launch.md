# /pm-butler launch

Invoke **The Shipper** for deployment and release readiness.

Read Shipper section in `content/ensemble.md`. Checklist: rollback, feature flags, comms, monitoring. Update `STATUS.md` when Project Edition is active.
